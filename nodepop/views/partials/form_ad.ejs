    <div class="container">
    <div id="title_ad">
    <p><%= __('Create Ad') %></p>
    </div>
   
    <form method="POST" id="form_ad">
        <div class="form-group">
          <label for="nombre"><%= __('Product') %></label>
          <input type="text" class="form-control" id="nombre" placeholder="Enter name"
                name="nombre"
                value="">
        </div>
        <div class="form-group">
          <label for="precio"><%= __('Price') %></label>
          <input type="text" class="form-control" id="precio" placeholder="€"
                name="precio"
                value="">
        </div>
        <div class="form-group">
            <label for="nombre"><%= __('Sell') %>?</label>
            <select name="venta">
                <option value="true"><%= __('Yes') %></option> 
                <option value="false"><%= __('No') %></option>
            </select>
        </div>
        <div class="form-group">
          <label for="foto"><%= __('Image') %></label>
          <input type="text" class="form-control" id="foto" placeholder="<%= __('Image Path') %> Ej: /Users/siono/Pictures/libro.jpg"
                name="foto">
        </div>
        <button type="submit" class="btn btn-primary"><%= __('New') %></button>
        
      </form>
      
      <div class="error"><%= error %></div>
      
      </div>